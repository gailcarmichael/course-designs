// This is the equivalent of representing a character as
// a struct (or class) in C++. In Java we define each
// class in a different file.
public class Character // <- notice in Java, the style is to
                       //  capitalize the names of classes
{
    String name; // note the capital S for String
    int hitPoints;
    int maxHitPoints;
}